# FreeContext optimization history

This file is the durable, Git-tracked history for the FreeContext skill. The current `skills/freecontext/SKILL.md` and `skills/freecontext/agents/openai.yaml` are also Git-tracked. Keep this history beside them so a skill change and its evidence can be reviewed together.

## Maintenance rules

Append one entry in the same change whenever `SKILL.md`, `agents/openai.yaml`, or a harness policy that directly changes skill routing or invocation behavior is modified. Record the evidence source and distinguish live benchmark results, local proof, inference, and proposal. Never write a proposal as verified, and never describe a small-sample result as a stable gain or loss. A commit is only a version locator; it is not semantic proof of correctness, authorization, completion, or release readiness.

Do not copy early task or Slice execution logs into this file. Keep only reusable routing, contract, validation, benchmark, and complete-trace findings. If a later change supersedes a conclusion, retain the old entry and state the new boundary explicitly.

## Entry template

### YYYY-MM-DD — short change title

- 日期：YYYY-MM-DD
- commit/source：`<commit or source>`; use the commit only to locate the version.
- 目标/问题：What evidence gap, routing failure, or contract problem was being addressed?
- 通用改动：What generic skill, manifest, harness-policy, or contract behavior changed?
- 验证范围：List local proof and, when applicable, the exact live benchmark denominator and settings.
- 关键 benchmark 数据：Report denominators, reward, token, FC-increment, wall, and call-layer counts with control/treatment labels where applicable.
- 完整轨迹观察：Report call timing, reentry/recovery behavior, waiting/concurrency, and concrete trace observations.
- 结论/下一步：State what the evidence supports and the next authorized or proposed step.
- 已知边界：State sample-size, attribution, instrumentation, environment, or authorization limits.

## Recorded stages

### 2026-08-30 — Release baseline

- 日期：2026-08-30
- commit/source：`26ad344` (`v0.1.0-rc.1` release baseline); current Git history, the active Program/decision record in `.work/orchestrator/note.md`, and the baseline audit notes.
- 目标/问题：Establish the starting FreeContext contract and the evidence that motivated conditional routing and later reentry work.
- 通用改动：This release commit is a version baseline, not a routing optimization. At this point the tracked skill described FC-first eligibility for broad or multi-file work, and both the skill and agent manifest were already tracked.
- 验证范围：Historical live baseline evidence was used for diagnosis; it is not a benchmark of this release commit in isolation and is not a causal claim about the release.
- 关键 benchmark 数据：The completed 240-trial baseline had 120/120 treatment trials call FC before native repository exploration, zero valid post-edit/post-check reentry, 100 audited post-edit cross-file exploration events without FC, and 119 schema, 50 intrinsic, and 30 chain rejections.
- 完整轨迹观察：FC waiting and downstream consumption were already functioning. The recurring issue was route selection and later route reevaluation, not concurrent native work during a provider wait.
- 结论/下一步：Use the current evidence gap, rather than task size alone, as the routing decision. Preserve the atomic wait, read-only ownership, and strict continuation/recovery validation.
- 已知边界：The baseline campaign is historical diagnostic evidence with a different denominator and source state; it must not be pooled with later paired campaigns as if it were a matched treatment effect.

### 2026-08-30 — Conditional evidence-gap routing

- 日期：2026-08-30
- commit/source：`f5bd3e6` (`feat: enable conditional FreeContext reentry`); source and authority are recorded in `.work/orchestrator/note.md` and the implementation Program.
- 目标/问题：Remove automatic FC-first routing for every broad task and make initial and later calls depend on the concrete evidence gap.
- 通用改动：The skill and manifest now route precise bounded evidence directly and reserve FC for cross-module, multi-role, long-document, multi-document, or source-bound gaps. MCP eligibility/tool wording and partial-result guidance were aligned, and the DeepSWE treatment policy no longer mandates FC-first. Wire schemas and strict typed reentry/recovery validation were preserved.
- 验证范围：Local proof passed focused behavior tests 65/65, the configured full suite 223/223, Gigatoken qualification, typecheck, build, static checks, skill validation, adapter syntax, and diff-check. A live paired benchmark ran five tasks with three repetitions per arm, 30 trials total, no retry, and max dynamic concurrency four.
- 关键 benchmark 数据：Control and treatment each solved 9/15. Treatment main-Agent reasoning-excluded tokens were 6.4% lower, but FC increment made total-system tokens 4.4% higher and wall time 7.5% higher.
- 完整轨迹观察：All 15 treatment traces still called FC before native exploration; valid typed reentry, post-edit calls, and post-check calls were zero. All 21 provider calls were synchronous waits with no concurrent main-Agent work, and all 13 usable Evidence results were consumed. A complete audit confirmed three of 15 traces with mid-session opportunities (Effect r2 and Scriggo r2/r3), with Scriggo r1 uncertain; the other 11 had no confirmed opportunity.
- 结论/下一步：The change established conditional wording but did not make later route reevaluation reliable. It does not prove a reward or token benefit. The next generic step was a post-edit/failed-check diagnostic checkpoint, without forcing a second call.
- 已知边界：Five tasks and three repetitions per arm are descriptive only. The confirmed opportunities identify routing misses, not counterfactual FC benefit.

### 2026-08-31 — Diagnostic checkpoint and arm alignment

- 日期：2026-08-31
- commit/source：`3e66293` (`fix: reinforce FreeContext diagnostic reentry`); the complete checkpoint decision and authority are in `.work/orchestrator/note.md`.
- 目标/问题：Make the post-edit/failed-check route explicit: read the exact failure location at most once, then classify before a second non-adjacent or cross-module diagnostic read.
- 通用改动：Added a generic checkpoint to the skill and paired DeepSWE harness. Both benchmark arms received the same checkpoint through the isolated global policy layer; only the final route differed. The treatment route allowed FC only for a distinct new static cross-module relationship not covered by the prior handoff. No schema or validator relaxation was made.
- 验证范围：Local proof passed focused routing/context tests 18/18, the configured full suite 223/223, typecheck, build, static checks, skill validation, adapter proof/syntax, and diff-check. A live exact-12 paired benchmark used the two tasks, three repetitions per arm, paired seeds, attempt 1, no retry/replacement, and max dynamic concurrency six.
- 关键 benchmark 数据：Control solved 2/6 and treatment 1/6. Treatment used 5.7% more main-Agent tokens, 11.9% more total-system tokens, and 13.8% more wall time. The treatment made 17 FC attempts, including 10 schema-accepted, 6 chain-accepted, and 9 provider-executed attempts.
- 完整轨迹观察：All 12 sessions loaded the common checkpoint and correct arm route, yet typed reentry remained zero. Two treatment traces had clear eligible cross-module post-check episodes, but the main Agent continued natively; the remaining traces lacked a clean distinct continuation. Provider waits were synchronous, with no native work during execution.
- 结论/下一步：The checkpoint clarified the intended route but did not establish actual typed reentry. Do not merge or release this stage as a successful reentry fix. The next generic correction was to distinguish same-question replay from a newly concrete child blocker and simplify valid caller shapes.
- 已知边界：The exact-12 denominator is noisy and cannot establish a stable treatment effect. Zero reentry means the reentry constraints were not tested on a real later call.

### 2026-08-31 — Typed continuation children and recovery-shape repair attempt

- 日期：2026-08-31
- commit/source：`c0a98d9` (`fix: distinguish FreeContext continuation children`); evidence is the exact-12 complete audit at `.work/freecontext-continuation-contract-2x3-paired-20260831/summaries/full-trace-audit.md` and the active Program record.
- 目标/问题：Keep identical replay invalid while accepting a distinct, explicitly parented child blocker exposed by Evidence consumption, an edit, or a check; reduce malformed continuation and recovery calls.
- 通用改动：Added typed `handoff_child` and `gap_concretization` derivations, normalized addressed-target/scope/fact replay checks, and simpler caller guidance. The attempted recovery simplification was still partly caller-reconstructed rather than genuinely session-loaded; no task-specific wording or forced call count was introduced.
- 验证范围：Local proof passed the configured full suite 225/225 plus focused continuation/recovery/history tests, typecheck, build, static checks, skill validation, adapter syntax, and diff-check. The live exact-12 campaign completed all 12 runner terminals with no retry or cleanup failure, using control/treatment repetitions 1–3 for Scriggo and Effect SSE at max concurrency six.
- 关键 benchmark 数据：Control=treatment=2/6. Treatment used 3.0% fewer main-Agent reasoning-excluded uncached tokens and 3.4% fewer visible tokens; FC added 145,748 provider-native tokens, total-system tokens rose 2.7%, and wall rose 0.6%. The FC attempt layers were 14 attempted / 13 schema-accepted / 12 intrinsic-accepted / 8 chain-accepted / 8 committed / 8 provider-executed.
- 完整轨迹观察：All provider calls occurred before the first edit. Typed `handoff_child` and `gap_concretization` reentry were both zero. Several Scriggo post-check episodes crossed compiler, emitter, VM, and interface-representation files without FC, confirming a policy miss rather than absence of eligible work. Calls were atomic waits with no native work during provider execution. Recovery traces showed that callers still resent the original request facts and produced avoidable malformed attempts.
- 结论/下一步：The campaign is valid evidence but the behavioral objective was not met; do not merge or release `c0a98d9` as a successful continuation fix. The next local correction was to make recovery session-loaded and recovery-only, and to broaden the post-check relation wording without forcing a second call.
- 已知边界：The six paired observations are descriptive only. The audit also found a strict call/session correlation mismatch that must not relabel valid runner terminals or provider executions; cross-campaign comparisons are not causal.

### 2026-08-31 — Session-restored recovery and provenance correction

- 日期：2026-08-31
- commit/source：`bee6fce` (`fix: restore FreeContext recovery context`); source and local authority are recorded in `.work/orchestrator/note.md` and `.work/orchestrator/CODEX_STATE.md`.
- 目标/问题：Finish the recovery contract exposed by the exact-12 audit and repair false provenance rejection without changing provider behavior or adding task-specific routing.
- 通用改动：Recovery now accepts only `priorSessionId` plus one workspace-relative `probePath`; the server restores immutable facts from the latest eligible committed `not_found` session and rejects overrides, chaining, wrong workspace, stale sessions, handoff results, and invalid paths before provider execution. The diagnostic route now covers runtime, type, data, control-flow, and owner relations after one exact failure read. Fresh provenance accepts a matching structured session id, unique exact serialized session reference, or unique exact call-id route, while missing or ambiguous evidence remains fail-closed.
- 验证范围：Focused tests passed 49/49 and the configured full suite passed 229/229 with zero skip/failure; typecheck, build, static checks, skill validation, adapter syntax, and diff-check passed. No live benchmark, provider, Pier, Docker, or network run was authorized or performed for this correction.
- 关键 benchmark 数据：No new benchmark data belongs to `bee6fce`. The latest live exact-12 numbers remain the `c0a98d9` record above and must not be attributed to this local-only correction.
- 完整轨迹观察：The correction is motivated by the c0 audit: all eight provider-executed calls were before the first edit and typed child reentry was zero, while Scriggo contained eligible post-check cross-module episodes. Local tests exercise recovery-only restoration, no-chain and fail-closed checks, and correlation matching; they do not demonstrate future live reentry.
- 结论/下一步：`bee6fce` is qualified local feature-branch evidence only. Preserve the exact-12 audit, keep the current skill behavior unchanged by this history file, and require a separately authorized benchmark before making any performance or reentry claim.
- 已知边界：No live validation exists for the corrected contract. Cross-process servers sharing one session directory still lack an atomic recovery claim; isolated per-trial session directories are unaffected. Merge, release, retry, replacement, and another live campaign remain outside the recorded authority.

### 2026-08-31 — Three-arm live reentry qualification

- 日期：2026-08-31
- commit/source：`00b182f` documentation tip with `bee6fce` as the latest behavior commit; the retained baseline index is `.work/freecontext-benchmark-baselines/reentry-five/README.md` and v1 live evidence is under its `baseline-v1-three-arm-20260831/` directory. The previous-skill arm used the two tracked skill files from `v0.1.0-rc.1/26ad344` while all arms shared the current product, server, harness, runtime, model settings, and paired seeds.
- 目标/问题：Test whether the session-restored recovery and continuation policy produces real post-edit or post-check reentry in long sessions, and compare it with both no FC and the previous release skill without changing task-specific behavior.
- 通用改动：No skill or product behavior was changed for this entry. It records a fresh three-arm qualification of the existing generic policy so later optimization can distinguish behavioral proof from performance claims.
- 验证范围：Five historically positive-control tasks, three repetitions per arm, 45 fresh trials total, max total concurrency six, attempt 1, no retry or replacement. All 45 result, aggregate, trajectory, verifier, and cleanup records passed root cross-check; the cost report rebuilt exactly from retained artifacts.
- 关键 benchmark 数据：Control, new skill, and previous skill each solved 6/15. Relative to control, the new skill used 3.16% more main-Agent reasoning-excluded uncached tokens, 18.50% more total-system tokens, and 17.54% more cumulative wall time. The previous skill used 0.90%, 13.05%, and 14.26% more respectively. New versus previous total-system tokens were 4.82% higher. New made 41 gather dispatches, 26 schema-accepted calls, and 22 provider-executed calls; previous made 33 tool dispatches, 32 reached MCP, and 19 executed the provider.
- 完整轨迹观察：The new arm produced one genuine process reentry in Pest r3 after the first edit and check. Step 27 accepted a typed `gap_concretization`, step 28 recovery returned ready Evidence for generator and parser-state seams, and steps 29 and 32 directly applied the result to generator and VM code; the trial still had reward 0. Obtaining that usable continuation required six post-check dispatch attempts, exposing caller-shape friction. The previous arm produced no process reentry. All four asynchronous calls across FC arms were explicitly waited to terminal completion. Evidence was consumed rather than ignored, but 11/12 new-skill Evidence calls and 13/13 previous-skill Evidence calls were followed by native rereads.
- 结论/下一步：The live behavior objective is minimally proven because one real post-check continuation completed and influenced edits. Performance and correctness improvement are not proven: aggregate reward was identical, main-Agent and total-system costs increased, and only one of 15 new-skill traces reentered. If work continues, simplify generic caller construction from retained session/handoff state and make post-check route reevaluation more reliable before another larger benchmark; do not add rules specialized to these five tasks.
- 已知边界：The task pool was deliberately selected from historical positive-control evidence and is not held out. Eight of 15 task-repetition groups had arm reward disagreement, so per-task outcome shifts are noisy. This entry does not authorize merge, release, publication, retry, replacement, or a new live campaign.

### 2026-08-31 — Prospective routing and compact continuation

- 日期：2026-08-31
- commit/source：Local feature work on `feat/fc-reentry-policy`, authorized by the prospective-routing and compact-continuation decision in `.work/freecontext-prospective-routing-compact-continuation-20260831/decision.md`.
- 目标/问题：Make the trigger prospective and low-pressure, and remove caller reconstruction of prior continuation state while preserving strict replay and session eligibility checks.
- 通用改动：The MCP description, tracked skill, manifest, and matched DeepSWE arm instructions now ask the Agent to notice likely repo-wide or substantial reading needs at roughly 30% confidence, with explicit checkpoints for unknown owners, runtime/type/data/control-flow tracing, and a second non-adjacent module after one exact read. Public continuation accepts only `priorSessionId`, one child question, a typed evidence/edit/check origin, and optional `knownRefs` or `parentGapId`; the server restores the committed context and maps omitted or supplied parents to handoff-child or gap-concretization relations. The recovery-only `{recovery:{priorSessionId,probePath}}` shape is unchanged.
- 验证范围：Root fresh proof passed 233/233 configured tests with Gigatoken on the Conda `test` interpreter, plus typecheck, build, the 69-production-module static check, skill validation, DeepSWE adapter syntax, and diff-check. Compact continuation is covered through the MCP handler, delivery correlation, and invocation provenance. No live benchmark, provider, Pier, Docker, or network validation was authorized or performed.
- 关键 benchmark 数据：No new live data exists and no performance or reentry-rate claim is made.
- 完整轨迹观察：The design is motivated by the retained evidence of caller-shape friction and missed prospective route checks; future behavior must be established from fresh complete traces rather than inferred from this contract change.
- 结论/下一步：This is a generic local contract and routing correction only. Review focused behavior proof and source-bound validation before any separately authorized campaign; do not add task-specific rules or force a second call.
- 已知边界：Continuation generations remain one-time and latest-session bound, but shared-directory cross-process claim races and live behavioral effectiveness are not established by local tests.

### 2026-08-31 — Prospective candidate v2 live validation

- 日期：2026-08-31
- commit/source：`e16b1e4` on `feat/fc-reentry-policy`; immutable evidence is under `.work/freecontext-benchmark-baselines/reentry-five/candidate-v2-prospective-20260831/`.
- 目标/问题：Validate whether prospective routing and compact continuation produce genuine task-process follow-up calls on the fixed five-task positive-control pool, without changing product behavior or specializing for those tasks.
- 通用改动：No product or skill behavior changed during this entry. It records one treatment-only live validation of the already-pushed `e16b1e4` policy.
- 验证范围：Five fixed tasks, three latest-FC repetitions each, 15 trials total, maximum concurrency six, attempt 1, no retry or replacement. All result, trajectory, verifier, cleanup, schedule, and runtime identities passed root review; campaign containers, networks, derived images, and ports were clean at terminal.
- 关键 benchmark 数据：Solved 6/15. Main-Agent reasoning-excluded uncached tokens were 3,053,921; FC added 454,587; total-system was 3,508,508; main visible o200k was 1,842,114; summed trial wall was 19,879 seconds; mean peak context was 137,921. Dispatch/schema/chain/provider counts were 26/24/18/20. Two chain-rejected calls had already executed the provider and consumed 35,566 FC tokens.
- 完整轨迹观察：Ofetch r1 produced two accepted `handoff_child` continuations after the initial FC result and focused native reads, so compact continuation can work during task exploration. Every dispatch in all 15 traces still preceded the first edit and first check; post-edit and post-check reentry were both zero. All three asynchronous calls were immediately waited with no parallel native work. Fourteen of 15 Evidence-bearing calls were followed by native reads or searches of cited paths; the remaining call formed a typed child without rereading its cited path.
- 结论/下一步：The live behavior goal is only partially met. Genuine pre-edit continuation occurred, but post-edit/check routing, context savings, speed improvement, and correctness improvement are not established. Do not run a larger campaign or claim release qualification. Any next change should address only the observed generic semantic-call construction and pre-provider chain-enforcement failures, followed by another small behavior check.
- 已知边界：There was no fresh control arm. Historical v1 comparisons use the same task/repetition seeds but are descriptive only: candidate and v1 new skill both solved 6/15, while candidate main, FC, total-system, visible, and wall totals were respectively 14.57%, 14.73%, 14.59%, 11.46%, and 10.91% higher.

### 2026-09-01 — Whole-phase routing candidate v3

- 日期：2026-09-01
- commit/source：Feature commit `7331efd10eeeb25c0e5121c4a3fdab3f24411b14` on `feat/fc-reentry-policy`; MCP policy id `freecontext-eligibility-v5`; benchmark label `candidate-v3-whole-phase-routing`. This supersedes the candidate-v2 next-command/30% wording without rewriting its frozen evidence.
- 目标/问题：The candidate-v2 traces and manual review showed that next-command heuristics could miss a later broad task phase or remain narrowed after one rejected continuation. The caller needs one simpler prospective decision that does not depend on task-specific file names or final call counts.
- 通用改动：Before starting or changing a task phase, judge the whole upcoming phase. Native repository reading is used only when one small bounded read is clearly enough; otherwise FreeContext is called before searching or reading. The same decision repeats after Evidence, an edit, or a check, and rejection affects only that request. The tracked skill, MCP description, and matched DeepSWE instructions use the same rule. Request schemas, recovery, replay checks, provider behavior, and waiting remain unchanged.
- 验证范围：Focused routing, MCP, and benchmark-context tests passed 37/37. The configured full suite passed 233/233 with Gigatoken through the Conda `test` interpreter and zero skip/failure; typecheck, build, 69-module static check, skill validation, adapter syntax, and diff-check passed.
- 关键 benchmark 数据：The directly authorized treatment-only run completed 15/15 valid trials at maxActive6 with retry/replacement/harness/cleanup failure zero and solved 4/15. Main-Agent/FC/total-system tokens were 3,028,003 / 563,745 / 3,591,748; main visible o200k was 1,684,937; delivered FC text was 41,707; summed wall was 18,289 seconds; mean peak context was 129,346.4. Calls were 47 attempted, 45 raw-schema accepted, and 36 provider executed; invocation kinds were 16 initial, 9 reentrant, 6 recovery, and 16 invalid.
- 四版本对比：For the fixed 15-trial denominator, no-FC / candidate-v1 / candidate-v2 / candidate-v3 solved 6 / 6 / 6 / 4; Main-Agent tokens were 2,583,766 / 2,665,449 / 3,053,921 / 3,028,003; FC tokens were 0 / 396,213 / 454,587 / 563,745; total-system tokens were 2,583,766 / 3,061,662 / 3,508,508 / 3,591,748; wall was 15,249 / 17,924 / 19,879 / 18,289 seconds. Only no-FC versus candidate-v1 was a fresh paired comparison; v2 and v3 are later treatment-only historical references.
- 完整轨迹观察：Root manually reviewed all four distinct post-phase calls. Three were typed reentries across FastAPI r2, FastAPI r3, and Pest r1; the fourth was FastAPI r2 recovery. FastAPI r2 received no Evidence, FastAPI r3 used one returned test path but still read broadly, and Pest r1 used ready generator spans to implement downstream match arms after a workspace check. All calls were fully waited. The aggregate post-edit/post-check counters overlap and must not be added as separate calls.
- 结论/下一步：Whole-phase routing achieved the narrow behavior target because task-process calls are no longer zero and one episode materially guided the next patch. It did not improve the overall result reliably. Against historical candidate-v2, Main-Agent tokens fell 0.85% and wall fell 8.00%, while FC tokens rose 24.01%, total-system rose 2.37%, and solved count fell from 6/15 to 4/15. Keep this feature-only; do not merge, release, or enlarge the benchmark from this evidence.
- 已知边界：There was no fresh concurrent control, and the five tasks are historical positive controls. Comparisons are descriptive only. The 16 invalid invocation classifications show continuing caller/continuation friction; future work must remain generic and must not specialize for these tasks.

### 2026-09-01 — Concrete-exploration routing and Pi contract candidate v4

- 日期：2026-09-01
- commit/source：Behavior commit `00139c726df3930b4d2e4e956c0de2e3158fb4e7` on `feat/fc-reentry-policy`; MCP policy id `freecontext-eligibility-v6`; intended benchmark label `candidate-v4-concrete-exploration`. This supersedes candidate v3 only as the next unqualified implementation candidate and does not rewrite any v1-v3 evidence.
- 目标/问题：Candidate v3 solved 4/15 while Main-Agent and total-system tokens were 3,028,003 and 3,591,748, respectively 17.19% and 39.01% above the historical no-FC denominator. It attempted 47 FC calls, executed the provider 36 times, and recorded 16 invalid invocations. Manual trace review also found frequent Pi-side invalid or no-progress tool use, so increasing call pressure again would risk both cost and correctness.
- 通用改动：The FC model remains `glm-5.3-flash` but uses `high` reasoning. Pi `glob.pattern` and `rg.glob` accept one optional string instead of arrays. Final evidence submission identifies an observed read by `observation_id` and lets the harness derive path, focus, role, and the canonical target; provider-facing packets no longer expose target IDs. The knownRef-first hard rejection is removed, while workspace confinement, sensitive-path exclusions, time/output/result budgets, and repeated no-progress blocking remain. Known references are priority hints rather than a prerequisite.
- 调用策略：Call only when the main Agent can state a concrete next multi-file or multi-relation repository exploration gap that native bounded reads cannot close. Exact paths, symbols, failures, one bounded read, edits, tests, checks, and diff/status remain native. A task start, phase change, apparent complexity, or probability threshold is not a trigger. Edit/check origins are preferred for later calls; Evidence-origin reentry is limited to an independent child needed before acting. One decisive implementation-owner verification read is allowed. Benchmark injection now points to the tracked skill and MCP description instead of repeating the trigger rule.
- 本地验证：The executor passed the directly affected focused tests, TypeScript typecheck/build/static check, skill validation, and diff check. Root then independently passed 100 directly affected tests, TypeScript typecheck, skill validation, and diff check. The complete suite, Gigatoken, replay, live provider/Pi, and benchmark were deliberately not run in this implementation round.
- 关键 benchmark 数据：The directly authorized treatment-only campaign completed 15/15 valid trials at maxActive6 with retry/replacement/harness/cleanup failure zero and solved 9/15. Main-Agent/FC/total-system tokens were 2,838,565 / 48,653 / 2,887,218; main visible o200k was 1,724,859; delivered FC text was 3,164; summed wall was 15,295 seconds; mean peak context was 128,635.3. There were only 2 attempted, accepted, and provider-executed FC calls. Both were `initial`, both returned `ready`, and both corresponding trials were unsolved; task-process reentry was zero.
- 五版本对比：For the fixed 15-trial denominator, no-FC / candidate-v1 / candidate-v2 / candidate-v3 / candidate-v4 solved 6 / 6 / 6 / 4 / 9. Main-Agent tokens were 2,583,766 / 2,665,449 / 3,053,921 / 3,028,003 / 2,838,565; FC tokens were 0 / 396,213 / 454,587 / 563,745 / 48,653; total-system tokens were 2,583,766 / 3,061,662 / 3,508,508 / 3,591,748 / 2,887,218; wall was 15,249 / 17,924 / 19,879 / 18,289 / 15,295 seconds. Candidate v4 used 91.37% fewer FC tokens and 19.62% fewer total-system tokens than candidate v3, but still used 9.86% more Main-Agent tokens and 11.74% more total-system tokens than the historical no-FC arm.
- 结论/下一步：The conservative rule clearly stopped overcalling: provider executions fell from 36 in candidate v3 to 2. It also produced the lowest total-system cost and wall time among the FC candidates, while solved count was the highest historical observation. However, it did not reduce Main-Agent tokens below no-FC or candidate-v1, and it completely lost the task-process reentry behavior that candidate v3 had finally demonstrated. Do not interpret 9/15 as an FC correctness gain or the two unsolved call trials as FC harm. A future decision must choose explicitly whether to validate sparse, high-confidence calls with a fresh control or restore generic reentry without returning to broad call pressure.
- 已知边界：Only no-FC versus candidate-v1 was a fresh concurrent paired comparison. Candidate v2-v4 were later treatment-only campaigns over historical positive-control tasks, and candidate v4 also changed FC reasoning from `low` to `high`; cross-version differences are descriptive rather than causal. The complete product suite and Gigatoken were not rerun after the candidate-v4 implementation, so this benchmark is behavior evidence rather than merge or release qualification. No task-specific rule or five-task exception was added.

### 2026-09-01 — Candidate v5 local whole-phase and caller-contract correction

- 日期：2026-09-01
- commit/source：Behavior commit `1f32a92` and qualification source `6edba36` on `feat/fc-reentry-policy`; policy id `freecontext-eligibility-v7`. The later live campaign used that exact pushed source and did not change historical campaigns.
- 目标/问题：The v4 15-trace audit showed that sparse calls came from an overly narrow single-gap interpretation, while required source areas could still be combined or caller coverage weakened by public target metadata. The correction keeps the decision generic and makes planned post-edit consistency audits eligible without forcing another call.
- 通用改动：Routing now judges the entire next source-understanding phase: a concrete question crossing non-adjacent owners or relationships calls before exploration, while one or two bounded reads and exact local actions remain native. Initial caller questions are role-plus-question items per required area; the server derives IDs, fact kinds, required defaults, and modes, with caller fixed to relationship plus exhaustive coverage. Explorer and finalization prompts require independent required-question coverage, exhaustive caller enumeration boundaries, and partial gaps. The routing text recognizes planned consistency-audit reentry through the existing typed edit/check contract without adding another eligibility API field. Direct invocation avoids catalog discovery, MCP tool/server descriptions are 1,000/721 Unicode characters, and duplicated benchmark trigger prose was not added.
- 15-trace审计关键数据：The complete v4 audit reviewed 15 traces: 12 had a clear missed initial call, one was borderline-to-clear, and two calls were correctly timed but under-covered. Post-edit consistency audits were especially visible in the Pebble trace; these observations motivate the generic whole-phase and planned-audit wording but do not establish live effectiveness.
- 本地验证：Root independently passed 101/101 focused tests and the configured 234/234 complete suite with Conda `test` Gigatoken enabled and zero skip/failure. Typecheck, build, the 69-production-module static check, skill quick validation, Python adapter syntax, and diff-check also passed.
- 关键 benchmark 数据：The directly authorized treatment-only campaign completed 15/15 valid trials at maxActive6 with retry/replacement/runner/harness/cleanup failure zero and solved 6/15. Main-Agent/FC/total-system tokens were 2,950,776 / 117,734 / 3,068,510; main visible o200k was 1,824,954; delivered FC text was 7,994; summed wall was 20,158 seconds; mean peak context was 135,269.9. All 15 recorded invocations were `initial`; statuses were failed12/partial2/ready1, only three executed the provider, and task-process reentry remained zero. Twelve trials had authoritative zero provider executions and therefore zero FC provider-token cost.
- 六版本对比：For the fixed 15-trial denominator, no-FC / candidate-v1 / candidate-v2 / candidate-v3 / candidate-v4 / candidate-v5 solved 6 / 6 / 6 / 4 / 9 / 6. Main-Agent tokens were 2,583,766 / 2,665,449 / 3,053,921 / 3,028,003 / 2,838,565 / 2,950,776; FC tokens were 0 / 396,213 / 454,587 / 563,745 / 48,653 / 117,734; total-system tokens were 2,583,766 / 3,061,662 / 3,508,508 / 3,591,748 / 2,887,218 / 3,068,510; wall was 15,249 / 17,924 / 19,879 / 18,289 / 15,295 / 20,158 seconds. Candidate v5 matched historical no-FC solved count but used 14.2% more Main-Agent tokens, 18.8% more total-system tokens, and 32.2% more wall time. Against v4 it solved three fewer, with 4.0% more Main-Agent tokens, 6.3% more total-system tokens, and 31.8% more wall time.
- 结论/下一步：The whole-phase wording raised recorded initial attempts from 2/15 in v4 to 15/15, but it did not produce any task-process reentry and only three calls reached the provider. Candidate v5 therefore did not achieve the intended cost or mid-session behavior objective. Do not respond by adding more trigger categories or task-specific rules. Before another behavior change, manually inspect the full traces to separate malformed or locally rejected requests from genuine missed reentry opportunities.
- 已知边界：Only no-FC versus candidate-v1 was a fresh concurrent paired comparison. Candidate v2-v5 were later treatment-only campaigns over historical positive-control tasks, so all cross-version deltas are descriptive rather than causal. No task-specific rule, compatibility layer, or public caller single override was added.

### 2026-09-01 — Candidate v6 soft finalization and evidence-consumption correction

- 日期：2026-09-01
- commit/source：Behavior commit `7af142f9bb6e91b683871d539b744adcc4c2547d` on `feat/fc-reentry-policy`; intended benchmark label `candidate-v6-soft-finalization`. This supersedes Candidate v5 only as the next unqualified implementation candidate and does not rewrite earlier benchmark evidence.
- 目标/问题：Candidate v5 could spend several minutes exploring and then lose all verified work at a terminal provider or deadline failure. Main-Agent instructions also permitted enough rereading to recreate the same repository map, while a terminal FC failure still forced a low-value exact probe.
- 通用改动：A one-shot 180-second soft deadline now waits for the active Pi batch or tool call to finish, then steers the same session into the existing submit-only finalizer. The finalizer asks for the strongest concise verified observations and explicit gaps, so incomplete work can return as partial Evidence. Steering is persisted in the retry context and emitted once. The 285-second hard deadline, 24-turn and 64-tool emergency ceilings, request/tool limits, model, reasoning depth, token policy, and call eligibility are unchanged.
- 消费与失败合同：Ready or partial Evidence is treated as already-read context. The main Agent may verify at most one or two necessary exact or adjacent contexts before editing or checking and must not remap the repository. A terminal `failed` result now routes directly to normal native exploration with no forced probe, recovery, or repeat FC call. `not_found` alone retains the existing exact-probe and once-only recovery path; the historical reader still accepts old failed-plus-exact-probe artifacts without making them current-valid.
- 本地验证：Root independently passed 105/105 directly affected tests and the configured 240/240 complete suite with Gigatoken through the Conda `test` interpreter, zero skip/failure, plus typecheck, build, the 69-production-module static check, Python adapter syntax, and diff-check. The full-suite audit exposed and then closed one finalizer retry-context regression before commit.
- 关键 benchmark 数据：The directly authorized treatment-only campaign completed 15/15 valid trials at maxActive6 with zero retry, replacement, runner failure, harness failure, or cleanup failure and solved 6/15. Main-Agent/FC/total-system reasoning-excluded uncached tokens were 2,803,646 / 157,727 / 2,961,373; main visible o200k was 1,762,081; delivered FC text was 9,840; summed wall was 20,310 seconds; mean peak context was 132,099.1. FC recorded 14 of 15 attempted calls, all recorded calls were initial, and five executed the provider. Results were partial4/failed10; one partial was confirmed as soft-deadline salvage, while the other three ended by stagnation or turn limit. No recorded accepted process reentry occurred.
- 七版本对比：For the fixed 15-trial denominator, no-FC / v1 / v2 / v3 / v4 / v5 / v6 solved 6 / 6 / 6 / 4 / 9 / 6 / 6. Main-Agent tokens were 2,583,766 / 2,665,449 / 3,053,921 / 3,028,003 / 2,838,565 / 2,950,776 / 2,803,646; total-system tokens were 2,583,766 / 3,061,662 / 3,508,508 / 3,591,748 / 2,887,218 / 3,068,510 / 2,961,373; wall was 15,249 / 17,924 / 19,879 / 18,289 / 15,295 / 20,158 / 20,310 seconds. Against v5, v6 kept solved count unchanged and reduced Main-Agent tokens 4.99%, total-system tokens 3.49%, main visible tokens 3.45%, and mean peak context 2.34%, while FC tokens rose 33.97% and wall rose 0.75%. Against no-FC, v6 kept solved count unchanged but used 8.51% more Main-Agent tokens, 14.61% more total-system tokens, and 33.19% more wall time.
- 结论/下一步：Soft finalization works in a live session but was observed once, so it is a reliability safeguard rather than an established performance gain. Candidate v6 is the best Main-Agent-token result since v1 among FC candidates, but it still misses the primary objective versus no-FC and produced no recorded accepted process reentry. Do not add call pressure or task-specific rules. Any next change needs trace-grounded evidence about why the Main Agent repeats work after receiving Evidence or why eligible mid-session calls are not made.
- 已知边界：A low-level transport throw before any valid assistant transcript remains a provider failure rather than spawning a speculative finalizer request. Only no-FC versus Candidate v1 was a fresh concurrent paired comparison; Candidate v2 through v6 are later treatment-only campaigns, so all cross-version differences are descriptive rather than causal.

### 2026-09-02 — Candidate v7 minimal text contract

- 日期：2026-09-02
- commit/source：This feature-branch change on `feat/fc-reentry-policy`; decision record `.work/fc-minimal-contract-candidate-v7-20260902/decision.md`. It replaces Candidate v6 as the next unqualified implementation candidate without changing any frozen benchmark result.
- 目标/问题：The complete Candidate v6 trace audit found that 11/15 attempts delivered no usable Evidence. Six provider/deadline paths never submitted, three useful candidates were rejected wholesale by observation/count validation, one useful candidate was erased by structured 8 KiB fitting, and one caller request failed the oversized public schema. No complete native source-understanding phase was replaced, while every partial result was followed by broad native rereading.
- 通用改动：The public request is now only `question`, optional `hints`, and optional `sessionId`. Ordinary FC assistant text is returned directly and is not parsed, format-validated, quota-fitted, or forced through `submit_evidence`. Optional continuation restores only the prior committed answer as context. The retired coverage, handoff, typed-reentry, recovery, eligibility, provenance, consumption, and structured-result machinery and its implementation-detail tests were deleted. DeepSWE integration and minimal cost/context export remain.
- 简短输出与降级：The FC system prompt carries Caveman-inspired compression guidance: answer first; preserve exact paths, symbols, numbers, commands, and errors; keep only decisive `path:line — symbol — fact` locations; remove filler, hedging, narration, raw logs, repeated facts, and long excerpts; use a short `Unknown` only when material. A 180-second soft steer asks the same worker to stop tools and answer from current findings. The 285-second hard boundary and a later provider failure preserve already emitted useful text instead of replacing it with an empty structured failure.
- 本地验证：Configured proof passed 74/74 tests with zero skip/failure, including Gigatoken `o200k_base` single/batch through the Conda `test` interpreter. Typecheck, clean build, the 58-production-module static check, DeepSWE adapter bytecode compilation, diff-check, and stale-build-artifact inspection passed. The build now removes only the repository's exact `dist/` before TypeScript compilation so deleted modules cannot survive as stale output.
- 规模结果：The tracked diff deletes roughly 16.9k lines and 34 retired paths while adding roughly 0.7k lines, including five focused replacement tests. Runtime capture no longer stores the same event stream twice and dead policy/overflow metrics were removed.
- 结论/下一步：Candidate v7 is locally qualified as a much smaller, deliberately breaking contract. It has no new provider, Pi, benchmark, correctness, token, or wall-time evidence. Any later live comparison must use a fresh direct authorization and treat prior v1-v6 sessions as historical artifacts rather than current continuation inputs.
- 已知边界：Prompt-only formatting is intentionally best-effort; precise wording or section shape is not a validity gate. Internal host envelopes still carry status, text, error code, session id, and optional session file for transport and debugging, but model text remains opaque. No merge, release, or publication is authorized by this entry.

### 2026-09-02 — Remove unused Candidate v7 answer inheritance

- 日期：2026-09-02
- 目标/问题：真实 Candidate v7 记录为 15/15 initial、0 continuation；上一轮 session answer inheritance 没有被使用，因此移除这条未使用的调用路径。
- 通用改动：公开 caller/MCP 请求收敛为必填 `question` 与可选 `hints`，严格拒绝 `sessionId`；删除 continuation 恢复函数、分支及其 session 扫描读取逻辑。内部 session 记录、结果身份字段和 benchmark 关联保持不变；需要上一轮信息时由主 Agent 直接放入新的 question/hints。
- 验证与边界：这是本地合同简化，没有新的 live benchmark 数据，不能据此声称性能收益。

### 2026-09-02 — Candidate v8 phase-aware differential routing

- 日期：2026-09-02
- commit/source：Candidate v8 feature change on `feat/fc-reentry-policy`; the exact immutable commit is recorded by the fresh campaign manifest after push. This entry does not create release or benchmark authority.
- 目标/问题：Candidate v7's diagnostic trace recorded 15/15 calls as initial and 0 as continuation, while its minimal text contract did not tell the caller how to reconsider a genuinely broader source-understanding question after an edit, check, or prior finding. The next correction must improve generic routing and answer consumption without restoring session state, typed reentry, or a result schema.
- 通用改动：The skill and MCP tool description now make the trigger phase-independent: before expanding repository reading, judge the whole source-understanding question. One or two known or changed files and exact compiler/test failures remain native, and a multi-file consistency audit remains non-local even when its paths are known. The same question is not repeated; a later call must target a genuinely new gap. Only facts that hints clearly describe as previously checked are settled; paths and symbols are leads that may be checked as needed. Returned FC facts are already-read navigation context, not automatically correct, so the main Agent opens only locations to edit, verify decisive or change-critical claims as needed, or resolve uncertainty instead of broadly replaying exploration. Differential auditing is for hints that describe prior reads or edits; an initial or no-prior call answers normally. The public request remains only `{question, hints?}`; no sessionId, continuation, typed reentry, or schema was restored. Calls remain single, exclusive, terminal waits using the explicit outer `wait` contract, and failed calls fall back directly to native exploration.
- 本地验证：Configured proof passed 75/75 tests with 0 skip/fail, plus typecheck, build, the 58-module static check, skill validation, Python harness syntax, and diff-check. The current exporter also replayed copied real Candidate v7 FastAPI and Updo artifacts and uniquely associated each committed FC session with its actual Codex `exec-*` call-end record. No live provider, benchmark, or network execution has been run for Candidate v8.
- 关键 benchmark 数据：The relevant Candidate v7 diagnostic is 15/15 recorded calls classified as initial and 0 continuation. Candidate v8 has no live benchmark data yet; no performance, correctness, cost, wall-time, or reentry-rate claim is made.
- 完整轨迹观察：No Candidate v8 live traces exist. The intended observable behavior is that a later call, when one is made, carries only a genuinely new gap and the worker answer is consumed as navigation context without broadly replaying exploration.
- 结论/下一步：The current user authorization covers a new 15-trial Candidate v8 campaign, but it has not started. Before launch, require clean pushed source plus fresh same-manifest and exporter proof; do not infer a gain from the v7 diagnostic denominator.
- 已知边界：Prompt guidance remains best-effort. The worker cannot enforce caller routing or prove that a path was actually read, and local tests cannot establish live reentry behavior or benchmark improvement.
