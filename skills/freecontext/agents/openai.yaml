interface:
  display_name: "FreeContext"
  short_description: "Concrete-gap routing for multi-file evidence searches"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "freecontext"
      description: "Read-only gather_context tool for multi-file and multi-document evidence"
