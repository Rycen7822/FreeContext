interface:
  display_name: "FreeContext"
  short_description: "Summarize captured command output before Main sees it"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "freecontext"
      description: "Tool-free gather_context output summarizer returning ordinary text"
