interface:
  display_name: "FreeContext"
  short_description: "Whole-phase concrete-gap routing for multi-owner evidence"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "freecontext"
      description: "Read-only gather_context tool for multi-file and multi-document evidence"
