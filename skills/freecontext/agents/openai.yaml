interface:
  display_name: "FreeContext"
  short_description: "Route cross-module repository questions to a concise read-only worker"
policy:
  allow_implicit_invocation: true
dependencies:
  tools:
    - type: "mcp"
      value: "freecontext"
      description: "Read-only gather_context tool returning ordinary assistant text"
